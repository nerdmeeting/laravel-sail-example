id: 70960
name: laravel-sail-example
environments:
    production:
        memory: 1024
        cli-memory: 512
        warm: 30
        runtime: 'php-8.4:al2'
        build:
            - 'COMPOSER_MIRROR_PATH_REPOS=1 composer install --no-dev'
            - 'php artisan event:cache'
            - 'npm ci && npm run build && rm -rf node_modules'
